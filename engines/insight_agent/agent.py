from engines.common.llm import  LLMClient
async def insight_agent_handler(
        role: str,
        query: str,
        task_id: str,
        llm_client: LLMClient,
        output_dir: str
):
    """
    私域找数据
    role:str
    query:str
    task_id:str
    llm_client:LLMClient
    output_dir:str

    :return:
    """
    pass
